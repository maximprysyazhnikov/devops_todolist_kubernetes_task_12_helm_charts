apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ printf "%s-pv" .Chart.Name }}
spec:
  capacity:
    storage: {{ .Values.pv.capacity }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.pv.hostPath | quote }}
  persistentVolumeReclaimPolicy: Retain
